set PATH ~/Documents/Development/Android_SDK/sdk/platform-tools/ $PATH
set -g -x PATH /usr/local/bin $PATH
set -g -x fish_greeting 'Welcome Sean'
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"

alias gs "git status"
alias g "git"
alias gc "git commit -am"
